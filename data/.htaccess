# Deny direct HTTP access to all files in this directory.
# PHP scripts read these files via the filesystem and are unaffected.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
